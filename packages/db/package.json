{
  "name": "@contact-vault/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"No lint configured yet\"",
    "db:generate": "echo \"Prisma schema lands in PR3 — skip generate\"",
    "db:migrate": "echo \"Prisma schema lands in PR3 — skip migrate\"",
    "db:studio": "echo \"Prisma schema lands in PR3 — skip studio\""
  },
  "dependencies": {
    "@contact-vault/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
